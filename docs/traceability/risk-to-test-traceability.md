# Risk-to-Test Traceability

Live mapping from `docs/business-risks/business-risk-register.md` to actual
test files. Updated every phase as tests are added — this is a ledger, not
aspirational.

| Risk ID | Test file | Layer | Status |
|---|---|---|---|
| RISK-01 (inventory overselling under concurrent orders) | `applications/inventory-service/src/test/java/com/fulfillx/inventoryservice/reservation/ConcurrentReservationIntegrationTest.java#exactlyStockCountReservationsSucceedUnderConcurrentAttempts` (20 concurrent 1-unit attempts against 5 units of stock -> exactly 5 successes, 15 controlled `INSUFFICIENT_INVENTORY` failures, zero overselling); also `InventoryApiIntegrationTest#databaseConstraintShouldPreventNegativeAvailableQuantity` / `#databaseConstraintShouldPreventNegativeReservedQuantity` (DB `CHECK` backstop) | Integration (Testcontainers PostgreSQL) | **Implemented** |
| RISK-02 (duplicate order submission) | `applications/order-service/src/test/java/com/fulfillx/orderservice/order/OrderPersistenceIntegrationTest.java#shouldRejectDuplicateIdempotencyKeyAtTheDatabaseLevel`; the same idempotency-key pattern applied to reservation creation: `applications/inventory-service/src/test/java/com/fulfillx/inventoryservice/reservation/ReservationApiIntegrationTest.java#shouldTreatRepeatedIdempotencyKeyAsSameReservationWithNoAdditionalSideEffect`, `#shouldRejectConflictingReuseOfIdempotencyKey` | Integration (Testcontainers PostgreSQL) | **Implemented** |
| RISK-07 (unauthorized access, foundation + first role-gated endpoints) | `applications/auth-service/src/test/java/com/fulfillx/authservice/auth/AuthApiIntegrationTest.java` — `shouldRejectLoginWithIncorrectPassword`, `shouldRejectLoginForDisabledUser`, `shouldRejectMeWithoutToken`, `shouldRejectMeWithTamperedToken`, `shouldRejectMeWithExpiredToken`, `shouldReturnCurrentUserForValidToken`; `applications/auth-service/src/test/java/com/fulfillx/authservice/security/JwtServiceTest.java` (expiry/tamper at the token layer); inventory-service's first role-gated business endpoints: `ProductApiIntegrationTest#shouldRejectProductCreationByCustomer`, `#shouldRejectProductCreationWithoutAuthentication`; `InventoryApiIntegrationTest#shouldRejectStockAdjustmentByNonAdmin`; `applications/inventory-service/src/test/java/com/fulfillx/inventoryservice/security/JwtServiceTest.java` (validates tokens signed by a different secret are rejected — proves local JWT validation is real, not a no-op) | Integration + Unit | **Partially implemented** — proves authentication is real, JWTs can't be forged or reused past expiry, and role-based authorization now gates real business endpoints in inventory-service; does not yet prove ownership-level authorization (e.g. a customer can't refund another customer's order — no refund endpoint exists) |
| RISK-15 (invalid state values, partial) | Database `CHECK` constraint `ck_orders_status` in `V1__create_orders_table.sql`; no dedicated test yet beyond implicit coverage via the persistence test. inventory-service's `inventory_reservations.status` `CHECK` (`RESERVED`/`RELEASED`) and its guarded transition are proven by `ReservationApiIntegrationTest#shouldTreatRepeatedReleaseAsIdempotentNoOp`. | Migration + Integration | **Partially implemented** — values only for orders, not transitions; inventory-service's small two-state reservation lifecycle is fully guarded |
| RISK-16 (monetary rounding, partial) | `OrderPersistenceIntegrationTest#shouldPersistOrderAndExposeItThroughFlywayMigratedSchema` asserts integer minor-unit storage round-trips correctly; `ProductApiIntegrationTest#shouldAllowAdminToCreateValidProduct` asserts the same for `products.price_minor` | Integration | **Partially implemented** — storage proven; rounding *rules* not yet exercised |
| N/A — data-integrity backstop for identity (not a numbered business risk; supports RISK-07's foundation) | `AuthApiIntegrationTest#shouldStorePasswordAsSecureHashNotPlaintext`, `#shouldEnforceUniqueEmailConstraintAtTheDatabaseLevel`, `#shouldRejectNonLowercaseEmailAtTheDatabaseLevel`; `OrderPersistenceIntegrationTest#shouldRejectOrderWithoutACustomerIdentityReference` (the practical substitute for a cross-service FK — see ADR-002) | Integration | **Implemented** |
| N/A — data-integrity backstop for the product catalog (supports RISK-01's foundation) | `ProductApiIntegrationTest#shouldRejectDuplicateNormalizedSku`, `#shouldRejectInvalidNegativePrice`, `#shouldRejectInvalidCurrencyFormat`, `#shouldNormalizeLowercaseCurrencyAndSku` | Integration | **Implemented** |
| All other risk IDs (RISK-03, 04, 05, 06, 08–14, 17, 18) | — | — | **Planned** — no test exists yet. Do not claim coverage. |

## How to keep this honest

When a test is added that protects a listed risk, add a row here in the
same commit. When a risk is identified that isn't in the register yet, add
it to `business-risk-register.md` first, then trace the test here. A risk
row with no test file is a gap, not a mistake — it just means the phase
that closes it hasn't happened yet.
